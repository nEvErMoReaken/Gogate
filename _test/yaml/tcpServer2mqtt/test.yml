test:
  chunks:
    - type: FixedLengthChunk
      length: 1
      sections:
        - from:
            byte: 1
          decoding:
            method: "DecodeByteToBigEndianBits"
          to:
            device: "traction_system.status"
            fields:
              - RIOM_sta_{1..8}







